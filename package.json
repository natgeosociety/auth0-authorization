{
  "name": "auth0-authorization",
  "version": "0.0.0",
  "dependencies": {
    "node-fetch": "2.3.0"
  },
  "devDependencies": {
    "typescript": "3.2.2"
  },
  "scripts": {
    "postinstall": "rm -rf lib; tsc --rootDir src --outDir lib --declaration"
  }
}
